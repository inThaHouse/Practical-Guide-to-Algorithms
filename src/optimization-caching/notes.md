# Optimization With Caching

- Memoization: caching the value that a function returns . (it's a type of caching)
- Caching is basically saving a value to an object or array.
- Caching is not saved on disk.
